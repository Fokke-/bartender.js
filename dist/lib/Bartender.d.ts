import type { BartenderOptions, BartenderBarOptions, BartenderPushElementOptions } from './types';
import { BartenderError } from './BartenderError';
import { Bar } from './Bar';
import { PushElement } from './PushElement';
export declare class Bartender {
    debug: boolean;
    readonly el: HTMLElement;
    readonly contentEl: HTMLElement;
    readonly switchTimeout: number;
    readonly bars: Bar[];
    readonly barDefaultOptions: BartenderBarOptions;
    private previousOpenButton?;
    private pushableElements;
    private queue;
    private resizeDebounce;
    private onBarUpdateHandler;
    private onKeydownHandler;
    private onResizeHandler;
    constructor(options?: BartenderOptions, barOptions?: BartenderBarOptions);
    destroy(removeBarElements?: boolean): Promise<this>;
    getBar(name: string): Bar | null;
    private getOpenBar;
    addBar(name: string, userOptions?: BartenderBarOptions): Bar;
    removeBar(name: string, removeElement?: boolean): Promise<this>;
    private openBar;
    open(name: string, button?: HTMLElement | null): Promise<Bar | Error>;
    private closeBar;
    close(name?: string): Promise<Bar | null>;
    toggle(name: string, button?: HTMLElement | null): Promise<Bar | BartenderError | null>;
    addPushElement(options?: BartenderPushElementOptions): PushElement;
    private pushElements;
    private pullElements;
    private onBarUpdate;
    private onKeydown;
    private onResize;
}
